// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // When you pick a host later, set `site` (and `base` for GitHub Pages
  // project sites). Nothing else needs to change.
  // site: 'https://toeesh.example',
  trailingSlash: 'ignore',
});
